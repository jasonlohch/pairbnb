<div>
<h1>
  <%= @listing.title %>
</h1>
 
<p>
  <strong>Location:</strong>
  <%= @listing.location %>
</p>

<p>
	<strong>Description:</strong>
	<%= @listing.description %>
</p>

<h3><%= link_to 'Edit', edit_listing_path(@listing) %> | <%= link_to 'Delete', listing_path(@listing), method: :delete, data: { confirm: 'Are you sure?'} %> | <%= link_to 'All Listing', listings_path %></h3>
</div>